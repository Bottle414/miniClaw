# 架构设计

## 整体架构

miniClaw 采用分层架构，各层职责单一、通过统一类型层解耦：

```
┌─────────────────────────────────────────────────┐
│                   入口层 (index.ts)              │
│              Agent Loop 主循环 / CLI 交互         │
├─────────────────────────────────────────────────┤
│                   接入层 (provider/)             │
│       Provider 初始化 / API 调用 / 流式输出       │
├─────────────────────────────────────────────────┤
│                   适配层 (adaptor/)              │
│    统一类型 ↔ 提供商类型 双向转换 / 流式 chunk     │
├─────────────────────────────────────────────────┤
│              ReAct 循环层 (react/)               │
│     Think-Act-Observe-Decide 循环 / 状态机       │
├─────────────────────────────────────────────────┤
│               记忆层 (memory/)                   │
│    记忆存储 / 上下文构建器 / 摘要压缩              │
├─────────────────────────────────────────────────┤
│              工具注册中心 (tools/)               │
│          工具定义注册 / 查找 / 执行调度            │
├─────────────────────────────────────────────────┤
│             消息处理中心 (utils/message)          │
│        工具调用解析 → 工具执行 → 结果回填          │
├─────────────────────────────────────────────────┤
│              配置层 (utils/config)               │
│     环境变量加载 / 校验 / RuntimeConfig 合并       │
├─────────────────────────────────────────────────┤
│               类型层 (types/)                    │
│  统一 LLM 类型 / 提供商类型 / 配置类型 / 事件类型   │
└─────────────────────────────────────────────────┘
```

## 各层介绍

### 入口层

**文件**: `src/index.ts`

Agent Loop 的主循环，负责：

- 加载环境变量与配置
- 初始化 Provider
- 维护对话消息历史
- 驱动用户输入 → LLM 调用 → 工具执行 → 回复输出的循环

支持两种循环模式：

#### 旧循环模式（递归模式）

```
用户输入 → 构造 UserMessage → provider.chat()
  → 返回 AssistantMessage
    → 有 toolCalls?
      → 是: 消息处理中心执行工具 → 构造 ToolMessage → 再次 provider.chat() (递归)
      → 否: 输出文本回复 → 等待下一轮用户输入
```

#### 新循环模式（ReAct 循环）

通过环境变量 `USE_REACT_LOOP` 控制（默认启用），实现显式的 Think-Act-Observe-Decide 循环：

```
用户输入 → 初始化 ReAct 状态
  → Think 阶段: 准备 LLM 调用
  → Act 阶段: provider.chat() → 解析响应
    → 有 toolCalls?
      → 是: Observe 阶段: 执行工具 → 记录观察结果 → Decide 阶段
      → 否: Decide 阶段: 检查终止条件
  → 应该终止?
    → 是: 返回最终答案
    → 否: 增加迭代次数 → 继续下一轮 Think
```

### ReAct 循环层

**文件**: `src/react/`

实现 ReAct (Reasoning + Acting) 模式的核心逻辑：

- **状态管理** (`state.ts`): 不可变状态对象，跟踪迭代次数、阶段、消息历史、行动记录、观察结果
- **终止逻辑** (`terminator.ts`): 多级终止检查（最终答案、迭代限制、错误、空响应）
- **循环编排** (`loop.ts`): 协调 Think → Act → Observe → Decide 四个阶段

```
ReActState {
  iteration: number          // 当前迭代次数
  phase: ReActPhase          // 当前阶段
  messages: LLMMessage[]     // 消息历史
  actionHistory: ActionRecord[]   // 行动记录
  observationHistory: ObservationRecord[]  // 观察记录
  shouldTerminate: boolean   // 是否终止
  terminationReason?: string // 终止原因
}
```

### 接入层

**文件**: `src/provider/`

Provider 封装了 LLM API 的调用细节，对上层暴露统一的接口：

- **chat** — 非流式调用，返回完整 `LLMResponse`
- **chatStream** — 流式调用，返回 `AsyncIterable<RuntimeEvent>`

每个提供商实现为一个函数+闭包：

- **DeepSeekProvider** — 持有 OpenAI 兼容客户端，内部调用 DeepSeek API
- 初始化时接收 Config，创建对应的 API 客户端
- 调用时委托适配层做类型转换，处理认证/限流/网络等错误

```ts
const provider = DeepSeekProvider()
provider.init(config)

// 非流式
const response = await provider.chat({ messages, model, tools })

// 流式
for await (const event of provider.chatStream({ messages, model, tools })) {
	// event: RuntimeEvent (text-delta / tool-call-start / tool-call-delta / finish / ...)
}
```

### 适配层

**文件**: `src/adaptor/deepseek/`

实现统一 LLM 类型与提供商特定类型之间的双向转换，是接入层与类型层之间的桥梁：

| 方向          | 方法                     | 说明                                                       |
| ------------- | ------------------------ | ---------------------------------------------------------- |
| 统一 → 提供商 | `transformRequest()`     | LLMRequest → DeepSeekChatCompletionRequest                 |
| 统一 → 提供商 | `transformTools()`       | LLMTool[] → DeepSeekTool[]，同时 `.` → `-`                 |
| 提供商 → 统一 | `transformResponse()`    | DeepSeekChatCompletionResponse → LLMResponse               |
| 提供商 → 统一 | `parseToolCalls()`       | DeepSeekToolCallResponse[] → LLMToolCall[]，同时 `-` → `.` |
| 流式 → 统一   | `transformStreamChunk()` | DeepSeekChatCompletionChunk → RuntimeEvent                 |

工具名中 `.` 与 `-` 的互转逻辑封装在此层，其他层禁止重复实现。

### 记忆层

**文件**: `src/memory/`

分层记忆管理，确保权威消息不可变，面向模型的上下文可按需裁剪：

- **MemoryStore** (`store.ts`): 权威消息存储，所有消息追加操作通过 `append()` 进行，原始 `messages` 不可变
- **上下文构建器** (`context-builder.ts`): 从权威消息生成面向模型的 `contextMessages`，支持保留、丢弃、注入、摘要四种操作
- **摘要器** (`summarizer.ts`): 将旧消息压缩为确定性摘要，节省上下文窗口。当前为简单摘要器，设计为可替换扩展点
- **类型** (`types.ts`): 记忆层相关类型定义

```
权威消息 (messages[])
  │
  ▼ 上下文构建器
  │  ├─ 保留: 保留近期 N 条消息
  │  ├─ 丢弃: 丢弃过旧或无关消息
  │  ├─ 注入: 注入系统消息
  │  └─ 摘要: 将旧消息压缩为摘要消息
  │
  ▼
面向模型的上下文 (contextMessages[])
```

### 工具注册中心

**文件**: `src/tools/`

ToolHandler 是工具的统一注册与调度中心，以 `Map<name, ToolEntry>` 维护工具表：

- **注册**: 每个工具导出 `{ definition, executor }`，在 `tools/index.ts` 中通过 `toolHandler.register()` 注册
- **查找**: `toolHandler.getToolDefinitions()` 返回所有工具定义，供 LLM 请求使用
- **执行**: `toolHandler.call(name, params)` 按名称调度执行，返回字符串结果

新增工具只需在 `tools/` 下创建文件并注册，无需修改其他层。

### 消息处理中心

**文件**: `src/utils/message.ts` + `src/utils/tool.ts`

负责将 LLM 返回的工具调用解析为可执行的操作，并回填结果：

1. **messageHandler** — 接收 LLM 响应中的 `tool_calls`，通过适配层的 `parseToolCalls()` 转为统一格式
2. **useTool** — 遍历工具调用列表，通过 `toolHandler.call()` 逐个执行，将结果构造为 `LLMToolMessage[]`

```
LLM 响应 tool_calls → parseToolCalls() → LLMToolCall[]
  → toolHandler.call() → 执行结果
    → 构造 LLMToolMessage[] → 追加到消息历史
```

### 配置层

**文件**: `src/utils/config.ts` + `src/prompts/system.ts`

配置分为两层：

| 类型          | 说明           | 字段                                                              |
| ------------- | -------------- | ----------------------------------------------------------------- |
| RuntimeConfig | 运行时持久配置 | systemPrompt, maxIterations, maxToolRetryTimes, maxSendRetryTimes |
| TaskConfig    | 单次任务配置   | baseURL, apiKey, model, tools, stream, soulPrompt, userPrompt     |

`createConfig(env)` 从环境变量加载配置，提供默认值并校验必要字段（apiKey 非空、baseURL 格式合法）。`Config` 类型为两者的交集。

`soulPrompt` 支持自定义角色人格，默认使用 `prompts/soul.ts` 中的 Soul 角色提示词。

### 类型层

**文件**: `src/types/`

定义与具体提供商无关的统一抽象类型，是各层之间的契约：

| 子模块             | 说明                                                                        |
| ------------------ | --------------------------------------------------------------------------- |
| `types/llm/`       | 统一 LLM 类型：LLMMessage, LLMRequest, LLMResponse, LLMTool, LLMToolCall 等 |
| `types/providers/` | Provider 接口 + 各提供商特定类型（含流式响应类型）                          |
| `types/config/`    | RuntimeConfig, TaskConfig, Config                                           |
| `types/event/`     | RuntimeEvent 流式事件类型（text-delta, tool-call-\*, finish 等）            |
| `types/react/`     | ReAct 循环类型（Phase, State, Action, Observation, Termination）            |

业务层禁止直接使用 `openai` 包的类型，必须通过类型层 + 适配层访问。

## 数据流

### 完整请求-响应流程（非流式）

```
用户输入 "上海天气"
  │
  ▼
构造 LLMUserMessage { role: "user", content: "上海天气" }
  │
  ▼ 追加到 messages[]
构造 LLMRequest { messages, model: "deepseek-chat", tools: [...] }
  │
  ▼ provider.chat(req)
  │
  ▼ adaptor.transformRequest(llmRequest)
  │   → DeepSeekChatCompletionRequest
  │   → messages 转换: LLMMessage[] → DeepSeekMessage[]
  │   → tools 转换: LLMTool[] → DeepSeekTool[] (工具名 "." → "-")
  │
  ▼ OpenAI 客户端调用 DeepSeek API
  │
  ▼ 收到 DeepSeekChatCompletionResponse
  │
  ▼ adaptor.transformResponse(deepseekResponse)
  │   → LLMResponse { id, created, model, message: { content, toolCalls } }
  │   → tool_calls 解析: "-" → "." 还原工具名
  │
  ▼ 返回 LLMResponse
  │
  ├─ 无 toolCalls → 输出文本回复
  │
  └─ 有 toolCalls → messageHandler 处理
      │
      ▼ parseToolCalls() → LLMToolCall[]
      │
      ▼ toolHandler.call("weather.getWeather", { city: "上海" })
      │   → "sunny"
      │
      ▼ 构造 LLMToolMessage { role: "tool", toolCallId, content: "sunny" }
      │
      ▼ 追加 assistant 消息 + tool 消息到 messages[]
      │
      ▼ 递归 provider.chat() → LLM 生成最终回复
```

### 流式输出流程

```
用户输入
  │
  ▼
provider.chatStream(req)
  │
  ▼ adaptor.transformRequest(llmRequest) → DeepSeek 请求格式
  │
  ▼ OpenAI 客户端流式调用 DeepSeek API
  │
  ▼ 逐 chunk 接收 DeepSeekChatCompletionChunk
  │
  ▼ adaptor.transformStreamChunk(chunk) → RuntimeEvent
  │   ├─ { type: "text-delta", content: "..." }     → 逐 token 文本输出
  │   ├─ { type: "tool-call-start", ... }            → 工具调用开始
  │   ├─ { type: "tool-call-delta", ... }            → 工具参数增量
  │   └─ { type: "finish", reason: "stop", usage }   → 生成完成
  │
  ▼ 业务层消费 RuntimeEvent，实时展示
```

### 类型转换流向

```
业务代码 (LLMRequest / LLMMessage / LLMTool)
  → adaptor.transformRequest()
    → 提供商请求格式 (DeepSeekChatCompletionRequest)
      → API 调用
        → 提供商响应格式 (DeepSeekChatCompletionResponse)
          → adaptor.transformResponse()
            → 业务代码 (LLMResponse)

流式:
提供商 chunk (DeepSeekChatCompletionChunk)
  → adaptor.transformStreamChunk()
    → 业务代码 (RuntimeEvent)
```

### 记忆层数据流

```
用户消息 / LLM 响应 / 工具结果
  │
  ▼ memoryStore.append()
  │  → 权威 messages[] 追加（不可变）
  │
  ▼ 上下文构建器 (每次 LLM 调用前)
  │  ├─ 保留近期消息
  │  ├─ 丢弃过旧消息
  │  ├─ 注入系统提示词
  │  └─ 摘要旧消息（可选）
  │
  ▼ 输出 contextMessages[]
  │
  ▼ adaptor.transformRequest() 使用 contextMessages 而非原始 messages
```

## 目录结构

```
src/
├── index.ts              # 入口，Agent Loop 主循环
├── provider/             # 接入层：Provider 实现
│   ├── index.ts          #   导出与类型重导出
│   └── deepseek.ts       #   DeepSeek Provider (函数+闭包, chat + chatStream)
├── adaptor/              # 适配层：类型转换
│   └── deepseek/
│       └── index.ts      #   DeepSeek 适配器 (transformRequest/Response/Tools/StreamChunk/parseToolCalls)
├── react/                # ReAct 循环层：推理+行动模式
│   ├── index.ts          #   ReAct 模块导出
│   ├── state.ts          #   状态管理（不可变状态）
│   ├── terminator.ts     #   终止逻辑（多级检查）
│   └── loop.ts           #   循环编排（Think-Act-Observe-Decide）
├── memory/               # 记忆层：上下文管理
│   ├── index.ts          #   记忆模块导出
│   ├── store.ts          #   MemoryStore 权威消息存储
│   ├── context-builder.ts #  上下文构建器（保留/丢弃/注入/摘要）
│   ├── summarizer.ts     #   确定性摘要器
│   ├── types.ts          #   记忆层类型
│   ├── context-builder.test.ts # 上下文构建器单测
│   └── summarizer.test.ts     # 摘要器单测
├── tools/                # 工具注册中心
│   ├── index.ts          #   ToolHandler 类 + 全局实例 + 工具注册
│   └── weather.ts        #   天气工具示例
├── utils/                # 工具函数
│   ├── config.ts         #   配置创建与校验
│   ├── message.ts        #   消息处理中心 (tool_calls → 工具执行 → ToolMessage)
│   ├── tool.ts           #   工具调用执行 (useTool)
│   ├── tool-message.ts   #   工具消息构造
│   ├── dotenv.ts         #   环境变量加载配置
│   └── logger.ts         #   受控日志系统 (infoType → 环境变量控制)
├── prompts/              # 提示词管理
│   ├── system.ts         #   系统提示词
│   ├── react.ts          #   ReAct 模式提示词
│   └── soul.ts           #   Soul 角色人格提示词
├── types/                # 类型层
│   ├── index.ts          #   类型导出入口
│   ├── llm/              #   统一 LLM 抽象类型
│   │   ├── message.ts    #     LLMMessage, LLMSystemMessage, LLMUserMessage, ...
│   │   ├── request.ts    #     LLMRequest
│   │   ├── response.ts   #     LLMResponse, LLMUsage
│   │   ├── tool.ts       #     LLMTool, LLMToolChoice
│   │   └── stream.ts     #     LLMStreamConfig
│   ├── providers/        #   Provider 接口 + 提供商特定类型
│   │   ├── index.ts      #     Provider, LLMAdapter 接口
│   │   └── deepseek.ts   #     DeepSeek 请求/响应/消息/流式 chunk 类型
│   ├── react/            #   ReAct 循环类型
│   │   ├── phase.ts      #     ReActPhase (thinking/acting/observing/deciding)
│   │   ├── state.ts      #     ReActState 接口
│   │   ├── action.ts     #     ActionRecord 行动记录
│   │   ├── observation.ts #    ObservationRecord 观察记录
│   │   ├── event.ts      #     ReAct 事件类型
│   │   └── termination.ts #    TerminationReason 终止原因
│   ├── event/            #   运行时事件类型
│   │   ├── index.ts      #     事件导出
│   │   └── runtime-event.ts #  RuntimeEvent (text-delta/tool-call-*/finish)
│   └── config/           #   配置类型
│       └── index.ts      #     RuntimeConfig, TaskConfig, Config
└── const/                # 常量
    ├── index.ts          #   常量导出
    └── path.ts           #   .env 文件路径
```
